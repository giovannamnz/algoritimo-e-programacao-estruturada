#include <stdio.h>

int main()
{

    // declaration of vectors
    int vector[6] = {1, 0, 5, -2, -5, 7};
    int sum;

    //computing 

    //sum between vectors
    sum = vector[0] + vector[1] + vector[5];

    //printing vector values
    printf("A soma dos vetores [0], [1] e [5] e: %i\n", sum);

    return 0;
}